# Render 블루프린트: 저장소를 연결하면 이 설정대로 무료 웹 서비스가 만들어진다.
services:
  - type: web
    name: the-other-side
    runtime: node
    plan: free
    buildCommand: npm ci --omit=dev
    startCommand: npm start
    healthCheckPath: /healthz
